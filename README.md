# AARI — Work of Kashmir

The published website. This repository contains the built site and is served
by GitHub Pages; the editable React + TypeScript source lives separately.

To publish an update, replace these files with a fresh `dist` build.
